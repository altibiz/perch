# Option

- `option.mkBranchOption`: Creates a flake output `branch` option.

- `option.mkIntegrationOption`: Creates a flake output `integrate` option. It
  also creates the appropriate systems, nixpkgs config and nixpkgs overlay
  options.
